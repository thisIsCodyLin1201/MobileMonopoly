# 🔐 安全配置指南

## 環境變數設定

本專案使用 `.env` 檔案來管理敏感資訊（如資料庫憑證），確保這些資料不會意外提交到版本控制系統。

## 設定步驟

### 1. 建立 .env 檔案

在專案根目錄下，複製 `.env.example` 並重新命名為 `.env`：

```bash
cp .env.example .env
```

### 2. 填入實際資料

編輯 `.env` 檔案，填入您的資料庫連線資訊：

```env
DB_URL=jdbc:postgresql://your-database-host:5432/your-database-name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
```

### 3. 驗證設定

確保 `.gitignore` 檔案中包含以下內容（已預設包含）：

```gitignore
# 環境變數檔案 - 包含機密資訊
.env
```

## 使用方式

專案中的 `Config.java` 類別會自動讀取 `.env` 檔案中的環境變數：

```java
// 載入環境變數
Config.loadEnv(".env");

// 讀取變數值
String dbUrl = Config.get("DB_URL");
String dbUser = Config.get("DB_USER");
String dbPassword = Config.get("DB_PASSWORD");
```

## 安全注意事項

⚠️ **請務必遵守以下規則**：

1. ✅ **永遠不要**將 `.env` 檔案提交到 Git
2. ✅ **務必提交** `.env.example` 作為範本
3. ✅ 使用強密碼和安全的資料庫連線
4. ✅ 定期更新憑證
5. ✅ 不要在程式碼中硬編碼任何敏感資訊

## 團隊協作

當團隊成員克隆專案後：

1. 通知他們需要建立自己的 `.env` 檔案
2. 透過安全管道（如加密訊息）分享憑證
3. 不要在 issue、PR 或聊天室中分享憑證

## 檢查清單

在推送程式碼前，請確認：

- [ ] `.env` 檔案已加入 `.gitignore`
- [ ] 沒有任何檔案包含明文憑證
- [ ] `.env.example` 已更新（如果有新的環境變數）
- [ ] README 已說明環境變數設定步驟

## 疑難排解

### 問題：無法載入環境變數

**解決方案**：
1. 確認 `.env` 檔案位於專案根目錄
2. 檢查 `.env` 檔案格式是否正確（KEY=VALUE）
3. 確保沒有多餘的空格或特殊字元

### 問題：Git 追蹤了 .env 檔案

**解決方案**：
```bash
# 從 Git 追蹤中移除（但保留本地檔案）
git rm --cached .env

# 確認 .gitignore 包含 .env
echo ".env" >> .gitignore

# 提交變更
git add .gitignore
git commit -m "Remove .env from version control"
```

## 更多資訊

如有任何安全疑慮，請立即聯繫專案維護者。
